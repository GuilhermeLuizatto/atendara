{
  "name": "atendara",
  "version": "0.1.0",
  "private": true,
  "description": "SaaS multiprofissional de gestao e automacao para profissionais de atendimento.",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "type-check": "next typegen && tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "npm run lint && npm run type-check && npm run test && npm run build",
    "test:rules": "node .local/firebase-tools/node_modules/firebase-tools/lib/bin/firebase.js emulators:exec --config firebase.emulator-tests.json --project demo-atendara --only firestore \"node scripts/test-firestore-rules.mjs\"",
    "test:repository": "node .local/firebase-tools/node_modules/firebase-tools/lib/bin/firebase.js emulators:exec --config firebase.repository-tests.json --project demo-atendara --only firestore \"vitest run --config vitest.emulator.mts\"",
    "test:emulator": "npm run test:rules && npm run test:repository && npm run test:access",
    "test:access": "node .local/firebase-tools/node_modules/firebase-tools/lib/bin/firebase.js emulators:exec --config firebase.access-tests.json --project demo-atendara --only auth,firestore,functions \"vitest run --config vitest.access.mts\""
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "date-fns": "^4.4.0",
    "firebase": "^12.18.0",
    "lucide-react": "^1.43.0",
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "tailwind-merge": "^3.6.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "@types/node": "^24",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9.39.5",
    "eslint-config-next": "16.3.4",
    "prettier": "^3.9.6",
    "prettier-plugin-tailwindcss": "^0.8.1",
    "tailwindcss": "^4.3.3",
    "typescript": "^5",
    "vitest": "^5.0.0"
  }
}
